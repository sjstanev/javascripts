function solved(name, age, avgGrade){
    // name = name.toString()
    // age = Number(age)
    // avgGrade = Number(avgGrade)

    console.log(`Name: ${name}, Age: ${age}, Grade: ${avgGrade.toFixed(2)}`)
}

solved('John', 15, 5.54678)
solved('Steve', 16, 2.1426)
solved('Marry', 12, 6.00)

